[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    scheduler: Scheduler compatibility tests
    inventory: Inventory module tests
    integration: Integration tests across multiple modules